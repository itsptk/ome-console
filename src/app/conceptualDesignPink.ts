/**
 * Single accent for “conceptual design” / prototype affordances.
 * Must match `ConceptualLabel` (“Conceptual design - Not for Implementation”).
 */
export const CONCEPTUAL_DESIGN_PINK = "#FF13F0" as const;
